{
  "name": "@kew/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Standalone backend: reads a real Redis via BullMQ and serves the QueueApi contract + the built dashboard.",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "seed": "bun src/seed.ts",
    "demo": "bun src/demo.ts",
    "feed": "bun src/test-feeder.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kew/core": "workspace:*",
    "bullmq": "^5.34.0",
    "hono": "^4.6.14",
    "ioredis": "5.10.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14"
  }
}
