{
  "name": "queue-panel",
  "private": true,
  "version": "0.0.0",
  "description": "Open-source dashboard for BullMQ queues and crons (codename).",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun --filter '@queue-panel/dashboard' dev",
    "build": "bun --filter '@queue-panel/dashboard' build",
    "typecheck": "bun --filter '@queue-panel/dashboard' typecheck"
  }
}
